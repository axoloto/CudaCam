#include <functional>
#include <iostream>

#include <docopt/docopt.h>

#include "cvPipeline.hpp"
//#include "logging.hpp"

int main(int, const char **)
{
  cvPipeline pipe;

  for (int i = 0; i > -1;)
  {
    int j = 0;
    ++j;
  }
}
